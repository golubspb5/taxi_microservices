## 🚕 **Сервис заказа такси (Taxi service, упрощённый: сетка N×M) (1-3)**

### Цель

Реализовать бэкенд-приложение заказа такси без реального геопоиска: город — это матрица `N×M`, у пассажиров и водителей есть целочисленные координаты `(x,y)`.

### Пользовательские сценарии
1. **Заказать поездку.** Пассажир задаёт координаты подачи `(xs,ys)` и назначения `(xd,yd)` → получает предварительное **расчётное время прибытия** (_ETA, Estimated Time of Arrival_) и цену → создаёт заказ.
2. **Статус водителя.** Водитель включает **online/offline** и указывает текущие координаты `(x,y)`.
3. **Назначение водителя.** Система назначает ближайшего доступного по **расстоянию по клеткам** — это число шагов по сетке только вверх/вниз/влево/вправо (формула: `|x1−x2| + |y1−y2|`). При равенстве — детерминированное правило (например, кто раньше стал online, или меньший id). Если водителей нет — заказ ставится в очередь.
4. Алгоритм ETA (expected time to arrive) и ценообразования продумать самостоятельно   
5. **Завершение.** После `completed` координаты водителя становятся равны точке высадки. Отмена разрешена до `passenger_onboard`.
6. Пассажир и водитель могут просматривать историю своих поездок
### Функциональные требования
- **Сетка:** размеры `N,M` конфигурируемы; координаты в пределах `[0..N-1]×[0..M-1]`.
- **Расстояние по клеткам:** считаем как `|x1−x2| + |y1−y2|`; `t_cell` — фиксированное время на один «шаг» по сетке.
- У водителя и у пассажира одновременно не более одной активной поездки.
- **Очередь и таймаут:** если водитель не подтвердил назначение за `T` секунд — назначается следующий/заказ остаётся в очереди.
- **Персистентность:** заказы, статусы, координаты водителей, тарифы сохраняются между перезапусками

### Нефункциональные ориентиры

- **Обновление статусов и ETA:** изменения видимы клиенту не позднее чем через ~2 секунды (поллинг/подписка — на выбор).
- **Масштаб:** DAU 10k, MAU 100k; до 1k водителей online.
- **Метрики:** число назначений/отказов, среднее время подачи, средняя длительность, отмены.